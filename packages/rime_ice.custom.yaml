# rime_ice.custom.yaml
# 雾凇拼音个性化补丁 —— 强化用户词典记忆/动态调频
# 该文件遵循 Rime 补丁规范，git 更新方案源码时不会被覆盖。
patch:
  # 明确开启用户词典（记录你打过的字词并动态调频）
  "translator/enable_user_dict": true
  # 开启自动调频：根据输入/选择频率调整候选项顺序
  "translator/enable_encoder": true
  # 提升用户词典的初始权重，让记忆过的词更容易排到前面
  "translator/initial_quality": 1.5
